# 🎮 LOL 对局文件自动上传器 - 零基础教程

> 完全不用写代码，复制粘贴、点按钮就行。

---

# 📦 第一部分：在你电脑上准备环境

## 1. 安装 Python

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 点击大大的黄色 **Download Python 3.12.x** 按钮
3. 下载完后运行安装程序
4. **⚠️ 最重要的一步**：安装界面 **最下面** 有个"Add Python to PATH"，**打勾勾**
5. 然后点 "Install Now"，等它装完

**验证：**
- 按键盘 `Win + R`，输入 `cmd`，回车
- 在弹出的黑窗口里输入：
  ```
  python --version
  ```
- 如果显示 `Python 3.12.x` 就成功了

## 2. 安装依赖

> 依赖 = 别人写好的功能包，不用你自己写

在黑窗口（CMD）里依次输入下面三条命令，每输完一条按一次回车，等它装完再输下一条：

```
pip install PySide6
```
```
pip install requests
```
```
pip install watchdog
```

每条安装可能需要 1-3 分钟，看到 `Successfully installed xxx` 就说明装好了。

---

# 🖥 第二部分：启动服务器

> 在你的服务器（或者你的电脑）上运行

## 1. 把服务器代码放到服务器上

把 `server` 文件夹里的 `app.py` 复制到你的服务器上（或者就在你自己电脑上也行）。

## 2. 安装 Flask

在黑窗口里输入：
```
pip install flask flask-cors
```

## 3. 启动服务器

打开 CMD，进入 `app.py` 所在的文件夹（比如放桌面了）：
```
cd C:\Users\你的用户名\Desktop
```

运行服务器：
```
python app.py
```

看到绿色字显示 `🎮 LOL 上传服务器已启动！` 就成功了。

**⚠️ 这个黑窗口不能关**，关了服务器就停了。你可以最小化它。

## 4. 测试服务器

用浏览器打开：
```
http://localhost:5000
```
看到显示 `{"status": "ok", "message": "LOL Uploader Server 正在运行"}` 就说明跑起来了。

## 📌 5. 搞清楚你的服务器地址

- **如果你在自己电脑上跑**：地址就是 `http://127.0.0.1:5000`
- **如果你在局域网另一台电脑跑**：在你的服务器上打开 CMD，输入 `ipconfig`，找到 `IPv4 地址`，比如 `192.168.1.100`
- **如果你在云服务器上跑**：就是云服务器给你的公网 IP

---

# 💻 第三部分：运行客户端

## 1. 修改服务器地址

用记事本打开 `client.py`，找到这一行：
```python
SERVER_URL = "http://你的服务器IP:5000"
```
改成你的真实服务器地址，比如：
```python
SERVER_URL = "http://192.168.1.100:5000"
```

## 2. 启动客户端

在 `client.py` 所在目录打开 CMD，运行：
```
python client.py
```

## 3. 使用流程

### 第一步：注册
![注册界面]
- 输入用户名（至少2个字符）
- 输入密码（至少6个字符）
- 先点「测试服务器连接」，确保能连上服务器
- 点「注册」

### 第二步：登录
- 输入刚才注册的用户名和密码
- 点「登录」

### 第三步：选择文件夹
- 点「选择文件夹...」
- 找到你的 LOL 对局文件存放位置
- 通常在这里：`C:\Users\你的用户名\Documents\League of Legends\Replays`

### 第四步：开始监视
- 点绿色的「开始监视」
- 按钮变成红色「停止监视」说明已经在工作了
- 去打一局英雄联盟，打完退出游戏后，文件会自动上传

### 查看日志
- 右侧黑色区域会显示所有操作记录
- 绿色文字表示上传成功

---

# 📦 第四部分：打包成 exe 发给朋友

## 方法一：完全不会报毒的方案（推荐）

> 需要一个数字签名证书，如果只是自己用可以跳过这步

用 Nuitka 编译（比 PyInstaller 更不容易报毒）：

1. 先装 Nuitka：
```
pip install nuitka
```

2. 选择一个杀毒软件**不监控的目录**，比如：
```
mkdir C:\myapp
cd C:\myapp
```

3. 编译：
```
python -m nuitka --standalone --windows-disable-console --onefile --output-dir=dist client.py
```
这会在 `dist` 文件夹生成一个 `client.exe`

4. 申请数字签名：
   - 去 https://www.ssl.com/ 买一个代码签名证书（约 ¥300-500/年）
   - 或者用免费的 [Azure Code Signing](https://learn.microsoft.com/en-us/azure/trusted-signing/)
   - 签名后，Windows 就不会报毒了

## 方法二：快速打包（但可能报毒）

```
pip install pyinstaller
pyinstaller --onefile --windowed --icon=icon.ico client.py
```
在 `dist` 文件夹里拿到 `client.exe`

## 如何减少报毒

| 方法 | 效果 | 难度 |
|---|---|---|
| 用 Nuitka 替代 PyInstaller | ⭐⭐⭐⭐ 很好 | 中 |
| 加数字签名 (¥300-500/年) | ⭐⭐⭐⭐⭐ 最好 | 低 |
| 提交到 [Microsoft Defender 门户](https://www.microsoft.com/en-us/wdsi/filesubmission) 申诉误报 | ⭐⭐⭐⭐ 免费 | 中 |
| 代码提交到 GitHub 让微软的自动化扫描学习 | ⭐⭐⭐ 一般 | 低 |
| 换个杀软（火绒/360 对 Python 程序友好些） | ⭐⭐⭐ 一般 | 低 |

---

# 🛠 第五部分：常见问题

## Q：连接不上服务器
- 检查服务器是否运行（CMD 窗口有没有关）
- 检查 `SERVER_URL` 地址是否写对了
- 如果是局域网，检查防火墙是否放行了 5000 端口

## Q：文件没有自动上传
- 确认文件夹选择正确
- 确认 LOL 的对局文件确实是 `.rolf` 结尾
- 看日志区有没有错误提示

## Q：不想用端口 5000
改 `server/app.py` 里的 `PORT = 5000`，改成你想要的数字，比如 `PORT = 8888`
然后客户端里的 `SERVER_URL` 也要对应改成 `http://xxx:8888`

---

# ✅ 最终检查清单

- [ ] Python 安装成功（`python --version` 有输出）
- [ ] 依赖都装好了
- [ ] 服务器跑了（浏览器打开 `http://localhost:5000` 有显示）
- [ ] 客户端的 `SERVER_URL` 改成了你的地址
- [ ] 注册了一个账号
- [ ] 选好了 LOL 对局文件夹
- [ ] 点「开始监视」后按钮变红了
- [ ] 去打了一局游戏，日志出现「上传成功」

---

**如果哪一步卡住了，直接告诉我，我帮你搞定！**
